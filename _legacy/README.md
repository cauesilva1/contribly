# Legacy apps

Código anterior (antes do rewrite unificado / marca Contribly), só referência:

- `projetomatch-client` — Next.js front separado
- `projetcmatch-back` — Express + Prisma

O app ativo está na raiz do repositório.
